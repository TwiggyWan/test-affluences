global:
    scrape_interval: 5s
    scrape_timeout: 5s
scrape_configs:
    - job_name: "todo-list"
      metrics_path: "/metrics"
      static_configs:
          - targets: ["host.docker.internal:3999"]
      authorization:
          type: "Bearer"
          credentials: "my_token"
